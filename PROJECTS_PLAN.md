# 项目规划文档

## 一、bili-agent 升级方向

### 当前状态（原版）
- Workflow 架构：关键词意图识别 → SQL路径 / 聊天路径
- 无 Function Call，无 RAG，无 Agent 循环
- 两次 API 调用（SQL生成 + 图表生成分离）

---

### 新架构：真正的 Agent

**删掉的部分（变成冗余）**

| 删掉的代码 | 原因 |
|-----------|------|
| `_DATA_KEYWORDS` 30+个关键词列表 | Claude 自己判断是否需要查数据 |
| `_is_data_question()` 函数 | 同上，意图识别交给 Claude |
| `if is_data / else` 路由分支 | 工具注册后 Claude 做路由 |
| 图表第二次 API 调用 | 合并进 Agent 循环，或规则判断 |

**新增的部分**

```
bili-agent/
├── app.py          ← 基本不动（SSE流式端点）
├── agents.py       ← 重构：只剩 Agent 循环 + 工具注册
├── tools.py        ← 新增：sql_query() + rag_search()
├── rag.py          ← 新增：BM25 + 向量检索 + RRF 融合
└── config.py       ← 新增向量模型配置
```

**Agent 核心流程**

```
用户问题
    ↓
注册两个工具：sql_query / rag_search
    ↓
Agent 循环（ReAct）：
  Claude 思考 → 决定调哪个工具 → 执行 → 看结果 → 继续或生成最终回答
    ↓
流式输出（含推理步骤 + 最终回答 + 图表配置）
```

**两个工具的职责**

| 工具 | 适用场景 | 底层实现 |
|------|---------|---------|
| `sql_query(sql)` | 精确数字、排名、聚合、过滤 | SQLite 执行 |
| `rag_search(query, top_k)` | 语义内容查找、话题匹配、模糊检索 | BM25 + 向量 + RRF |

**RAG 语料**：视频标题 + 简介文本，索引成向量（本地 sentence-transformers 模型，无需额外 API）

**SQL vs RAG 不冲突的原因**：关键词路由（Workflow）做路由会冲突；Function Call 让 Claude 做路由，自然选择最合适工具，甚至可以先 RAG 再 SQL 链式调用。

---

### 前端升级（bili_agent_preview.html 已设计）

| 区域 | 升级内容 |
|------|---------|
| 顶部 Tab | AI对话 / 数据看板 两个 Tab |
| 左侧 | 历史记录侧边栏，按日期分组，支持点击回看 |
| SQL 块 | Edit 按钮（contenteditable）+ 重新运行按钮 |
| 图表 | 类型切换：柱状图 / 折线图 / 饼图 / 隐藏 |
| 表格 | Markdown 复制、CSV 下载、保存图表、列排序 |
| 回答后 | "你可能还想问" 追问建议（3条） |
| 推理过程 | 可折叠面板：显示每步工具调用 + 状态图标 |
| 来源引用 | RAG 答案带 [1][2] 内联标注，悬停显示来源片段 |
| 数据看板 | 统计卡 + 分区播放量图 + 年份趋势图 + Top榜单 |

**新增回答类型标签**：`SQL精确查询` / `语义检索` / `混合模式` / `闲聊`

---

### 新增技术栈

```
原版：flask + anthropic
升级后新增：
  sentence-transformers   # 本地向量模型
  rank-bm25               # BM25 关键词检索
  numpy                   # RRF 分数融合计算
```

---

## 二、JobLens 招聘项目

### 项目定位
AI 驱动的求职分析工具，解决：
- 信息不透明（无法批量对比岗位）
- 判断慢（不知道自己是否符合 JD）
- 技能盲区（不知道差在哪里）
- 市场不透明（不知道行业薪资/技能分布）
- 面试没准备（不了解公司业务方向）

---

### 数据覆盖策略

**核心维护组合（约20个，长期保持）**

| | 北京 | 上海 | 深圳 | 杭州 |
|--|------|------|------|------|
| AI/大模型 | ✓ | ✓ | ✓ | ✓ |
| 后端开发 | ✓ | ✓ | ✓ | ✓ |
| 前端开发 | ✓ | ✓ | ✓ | — |
| 数据分析 | ✓ | ✓ | — | — |
| 产品经理 | ✓ | ✓ | — | — |

**其余组合**：申请开通模式，用户提交后进入异步爬取队列，完成后邮件通知

**数据刷新**：每晚异步任务，更新核心组合数据

---

### 15 大行业分类（含关键词映射）

| # | 分类 | 核心关键词 |
|---|------|----------|
| 1 | AI/大模型 | AI工程师、大模型、LLM、Agent、RAG、提示词工程 |
| 2 | 后端开发 | Java、Python后端、Go、Spring、微服务、接口开发 |
| 3 | 前端开发 | React、Vue、TypeScript、小程序、H5 |
| 4 | 数据分析 | 数据分析、BI、SQL、数据挖掘、Tableau |
| 5 | 算法工程师 | 推荐算法、搜索算法、NLP算法、CV算法 |
| 6 | 产品经理 | 产品经理、AI产品、C端产品、B端产品 |
| 7 | 运营 | 内容运营、用户运营、增长运营、数据运营 |
| 8 | 测试/QA | 测试工程师、自动化测试、性能测试 |
| 9 | DevOps/运维 | k8s、Docker、CI/CD、云原生、SRE |
| 10 | 安全 | 安全工程师、渗透测试、等保、漏洞挖掘 |
| 11 | 嵌入式/硬件 | 嵌入式、ARM、FPGA、单片机、驱动开发 |
| 12 | 游戏开发 | Unity、Unreal、游戏客户端、游戏服务端 |
| 13 | UI/设计 | UI设计、UX、交互设计、视觉设计 |
| 14 | 市场/销售 | 市场营销、商务拓展、销售、BD |
| 15 | 管理/其他 | 项目经理、技术总监、CTO、研发管理 |

---

### 核心功能设计

#### 1. AI 匹配评分
- 用户简历（文本）vs JD（文本）→ 向量相似度
- 输出：匹配分数（0-100）+ 匹配原因摘要
- 展示：岗位卡片右上角分数徽章

#### 2. 技能 Gap 分析
- LLM 对比简历技能 vs JD 要求技能
- 输出：
  - 已具备的技能
  - 欠缺但可快速补充的技能
  - 欠缺且需要长期积累的技能
  - 建议学习路径

#### 3. 公司情报
- 数据来源：JD 文本本身（最可靠，无需爬公司官网）
- 分析维度：
  - 技术栈偏向（从 JD 要求反推）
  - 业务场景（从 JD 描述提取）
  - 团队规模信号（融资阶段、团队描述）
  - 面试可能考点（从技术要求推断）
- 适用范围：中小公司无官网，大公司 JD 也包含核心信息

#### 4. 市场洞察
- 各技能出现频率排行
- 薪资区间分布（按城市/行业）
- 高频技能词云
- 学历/经验要求分布

#### 5. 简历管理
- 无需登录，localStorage 持久化
- 支持文本直接编辑 + PDF 上传解析（pdfplumber）
- 简历更新后自动重新计算所有已浏览岗位的匹配分

---

### 前端页面（7个）

| 页面 | 核心内容 |
|------|---------|
| 数据仪表盘 | 城市/行业筛选器 + 统计卡 + 热门技能 + 数据新鲜度 |
| 岗位列表 | 卡片流 + AI匹配分 + 多维筛选 + 排序 |
| 岗位详情 | JD全文 + 公司情报面板 + Gap分析 + 面试建议 |
| 我的简历 | 文本编辑器 + PDF上传 + 技能标签 |
| 市场洞察 | 薪资分布 + 技能热度 + 趋势图 |
| AI 对话 | 自由问答（"哪些公司在用RAG？"） |
| 数据覆盖 | 覆盖矩阵（城市×行业）+ 申请开通入口 |

---

### 技术架构

**后端**
```
FastAPI（主框架）
├── /api/jobs        # 岗位列表/详情
├── /api/match       # 简历匹配评分
├── /api/gap         # Gap分析
├── /api/insights    # 市场洞察
├── /api/ask         # AI对话（SSE流式）
└── /api/request     # 申请开通提交
```

**存储**
```
SQLite          # 岗位结构化数据（标题、薪资、公司、城市、行业）
Qdrant          # 向量索引（JD全文向量，用于语义搜索和匹配）
```

**AI 能力**
```
Claude API      # Gap分析、公司情报提取、AI对话
sentence-transformers  # 本地向量模型（简历+JD向量化）
rank-bm25       # 关键词检索
RRF融合         # 混合排序
```

**搜索引擎**
```
用户查询 → BM25分数 + 向量相似度 → RRF融合 → top-k结果
```

**数据爬取**
```
AsyncIO 异步任务
├── 每晚定时：更新核心20个组合
├── 按需队列：用户申请开通后触发
└── 反爬处理：模拟人机交互（已解决）
```

**部署**
```
Docker 容器化
腾讯云 4核8GB
Nginx 反向代理
```

---

### 新增技术栈（相比 bili-agent）

```
fastapi + uvicorn     # 替换 Flask，支持原生 async
qdrant-client         # 向量数据库
sentence-transformers # 本地向量模型
rank-bm25             # BM25 检索
pdfplumber            # PDF 简历解析
aiohttp               # 异步 HTTP（爬虫用）
apscheduler           # 定时任务调度
```

---

## 三、两个项目对比

| 维度 | bili-agent（升级后） | JobLens |
|------|-------------------|---------|
| 定位 | B站数据分析助手 | 求职辅助工具 |
| 数据 | B站视频元数据（SQLite） | Boss直聘招聘JD（SQLite + Qdrant） |
| 核心 AI 能力 | Text-to-SQL + RAG + Agent | 语义匹配 + Gap分析 + 公司情报 |
| 检索方式 | SQL精确 + RAG语义 | BM25 + 向量 + RRF混合 |
| Agent 架构 | Function Call + ReAct循环 | 部分 Agent（对话模块）|
| 框架 | Flask | FastAPI（原生async）|
| 向量存储 | 内存/本地 | Qdrant |
| 用户数据 | 无 | localStorage（简历）|
| 商业价值 | 自媒体创作决策工具 | 求职效率提升工具 |

---

## 四、简历技术栈覆盖

完成两个项目后，可展示的技术点：

- **Agent 架构**：Function Call、ReAct循环、多工具编排
- **RAG**：BM25 + 向量检索 + RRF融合、语义匹配
- **LLM 工程**：流式输出（SSE）、Prompt工程、结构化输出
- **向量数据库**：Qdrant、embedding索引
- **MCP**（可选扩展）：将 bili-agent 封装为 MCP Server
- **后端**：Flask、FastAPI、异步任务
- **前端**：ECharts可视化、响应式布局
- **部署**：Docker、Nginx

覆盖市场上 AI 工程师 JD 的主流要求。
