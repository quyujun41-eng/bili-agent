services:
  bili-agent:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./config.py:/app/config.py                          # 配置文件
      - ../bilibili.db:/bilibili.db                         # 数据库（容器内路径 /bilibili.db）
    restart: unless-stopped
