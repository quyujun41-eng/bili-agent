services:
  bili-agent:
    build: .
    container_name: bili-agent
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - ../bilibili.db:/bilibili.db
    restart: unless-stopped
