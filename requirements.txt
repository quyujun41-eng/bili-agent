# Web 框架
fastapi
uvicorn[standard]
jinja2

# LLM
anthropic
openai

# 向量数据库（与 Qdrant server 1.9.x 兼容）
qdrant-client==1.9.2

# Embedding（torch 由 Dockerfile 单独安装 CPU-only 版本）
sentence-transformers==3.4.1
transformers==4.44.2

# Redis
redis>=5.0.0

# Reranker
cohere>=5.0.0

# 关键字检索
rank-bm25
jieba

# 数值计算（<2 避免与 CPU-only torch 不兼容）
numpy<2

# 配置
pydantic-settings
